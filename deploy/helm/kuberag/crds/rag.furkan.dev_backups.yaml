---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.5
  name: backups.rag.furkan.dev
spec:
  group: rag.furkan.dev
  names:
    kind: Backup
    listKind: BackupList
    plural: backups
    singular: backup
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .spec.knowledgeBaseRef.name
      name: KB
      type: string
    - jsonPath: .status.totalPoints
      name: Points
      type: integer
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          Backup represents a point-in-time snapshot of a KnowledgeBase's vector store
          data, written to S3-compatible object storage.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: BackupSpec defines the desired state of Backup.
            properties:
              destination:
                description: Destination configures where the backup archive is stored.
                properties:
                  s3:
                    description: S3 configures an S3-compatible destination for the
                      backup archive.
                    properties:
                      accessKeySecretRef:
                        description: AccessKeySecretRef references a Secret key holding
                          the S3 access key.
                        properties:
                          key:
                            type: string
                          name:
                            type: string
                        required:
                        - key
                        - name
                        type: object
                      bucket:
                        description: Bucket to store the backup archive in.
                        minLength: 1
                        type: string
                      endpoint:
                        description: Endpoint is the S3-compatible server URL (e.g.
                          https://s3.amazonaws.com).
                        minLength: 1
                        type: string
                      prefix:
                        description: |-
                          Prefix within the bucket (e.g. "kuberag/backups"). Defaults to
                          "kuberag-backups".
                        type: string
                      region:
                        description: Region for the S3 bucket.
                        minLength: 1
                        type: string
                      secretKeySecretRef:
                        description: SecretKeySecretRef references a Secret key holding
                          the S3 secret key.
                        properties:
                          key:
                            type: string
                          name:
                            type: string
                        required:
                        - key
                        - name
                        type: object
                    required:
                    - accessKeySecretRef
                    - bucket
                    - endpoint
                    - region
                    - secretKeySecretRef
                    type: object
                required:
                - s3
                type: object
                x-kubernetes-validations:
                - message: s3 block is required
                  rule: has(self.s3)
              knowledgeBaseRef:
                description: |-
                  KnowledgeBaseRef points to the KnowledgeBase whose vector store data
                  should be backed up. Must exist in the same namespace.
                properties:
                  name:
                    type: string
                required:
                - name
                type: object
              suspend:
                description: |-
                  Suspend, when true, pauses the backup controller. Already-running
                  backup Jobs are not affected.
                type: boolean
            required:
            - destination
            - knowledgeBaseRef
            type: object
          status:
            description: BackupStatus defines the observed state of Backup.
            properties:
              activeJob:
                description: ActiveJob is the name of the in-flight backup Job, if
                  any.
                type: string
              backupID:
                description: BackupID is a unique identifier for this backup snapshot
                  (timestamp-suffix).
                type: string
              completionTime:
                description: CompletionTime is when the backup finished successfully.
                format: date-time
                type: string
              conditions:
                description: Conditions represent the current state of the Backup.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              location:
                description: Location is the S3 URI where the backup archive was written.
                type: string
              phase:
                description: Phase of the backup lifecycle.
                enum:
                - Pending
                - Running
                - Completed
                - Failed
                type: string
              sizeBytes:
                description: SizeBytes is the approximate size of the backup archive
                  in bytes.
                format: int64
                type: integer
              totalPoints:
                description: TotalPoints is the number of vector points included in
                  the backup.
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}

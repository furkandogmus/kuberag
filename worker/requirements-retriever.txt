fastembed==0.8.0
qdrant-client==1.18.0
psycopg[binary]==3.3.4
pymilvus==3.0.0
marshmallow==3.26.2
setuptools==80.10.2
openai==2.43.0
httpx==0.28.1
fastapi==0.138.0
uvicorn==0.49.0
pydantic==2.13.4
python-multipart==0.0.32     # /ingest/file Form uploads (playground dev mode)
requests==2.34.2         # /ingest/url web fetch (playground dev mode)
redis==8.0.0             # optional distributed Retriever rate-limit backend
pypdf==6.13.3             # PDF extraction for /ingest/file
# pillow pin: fastembed 0.4.2 requires pillow<11.0.0 >=10.3.0,
# so 12.x is unavailable. Trivy will flag CVE-2026-25990 etc.
# until fastembed upgrades.

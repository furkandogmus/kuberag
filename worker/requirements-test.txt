fastapi==0.115.6
httpx==0.28.1
pydantic==2.13.4
python-multipart==0.0.9  # /ingest/file Form route (app import)
pypdf==5.1.0             # PDF extraction tests (sources + ingest)
requests==2.32.3         # web-crawl / URL-ingest tests
pytest==8.3.4            # nightly integration tests

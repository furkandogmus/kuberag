fastapi==0.138.0
httpx==0.28.1
pydantic==2.13.4
python-multipart==0.0.32     # /ingest/file Form route (app import)
pypdf==6.14.2             # PDF extraction tests (sources + ingest)
requests==2.34.2         # web-crawl / URL-ingest tests
redis==8.0.1             # distributed rate-limit unit/integration tests
pytest==9.1.1            # nightly integration tests
pyyaml>=6.0.3            # hack/gen-api-docs.py reads CRD manifests

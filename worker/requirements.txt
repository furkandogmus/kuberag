# Core (always needed by the worker)
fastembed==0.8.0
qdrant-client==1.18.0
kubernetes==36.0.2

# Source backends
boto3==1.43.22          # s3
requests==2.34.2         # web crawl

# Optional vector stores (kept here so the single worker image is fully capable)
psycopg[binary]==3.3.4   # pgvector
pymilvus==3.0.0          # milvus
marshmallow==4.3.0     # environs (via pymilvus) needs marshmallow.__version_info__ (gone in 4.x)
setuptools==82.0.1     # pymilvus imports pkg_resources, removed from setuptools 81+

# Optional embedding provider (httpx pinned: openai 1.40.x breaks on httpx>=0.28)
openai==2.41.0
httpx==0.28.1

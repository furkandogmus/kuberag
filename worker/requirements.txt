# Core (always needed by the worker)
fastembed==0.3.6
qdrant-client==1.11.3
kubernetes==30.1.0

# Source backends
boto3==1.34.162          # s3
requests==2.32.3         # web crawl

# Optional vector stores (kept here so the single worker image is fully capable)
psycopg[binary]==3.2.1   # pgvector
pymilvus==2.4.6          # milvus
marshmallow<4            # environs (via pymilvus) needs marshmallow.__version_info__
setuptools<81           # pymilvus needs pkg_resources, removed from setuptools 81+

# Optional embedding provider (httpx pinned: openai 1.40.x breaks on httpx>=0.28)
openai==1.40.6
httpx==0.27.2
